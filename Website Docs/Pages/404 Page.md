# 404 Page

**Source**: `_input/404.html` · **URL**: `/404.html` (Netlify serves it for any missing route) · **Layout**: `default.html`

Minimal: "404 — Page not found" + a **Go home** button linking to `/` (which then language-redirects, see [[Build & Deploy]]). Styled by `.page-404` in [[pages.css]] (centered column with nav clearance, shared with the contact thank-you pages).

Because it uses `default.html`, the nav's language detection falls back to English ([[Languages & i18n]]).

Related: [[Home]]
