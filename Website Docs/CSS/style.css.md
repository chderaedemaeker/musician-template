# style.css

**Not loaded by the site.** A comment-only stub (16 lines) left behind when the original monolithic stylesheet was split into the per-concern files. It documents the split and points to `css.liquid` for the import order — the same map as [[CSS Overview]].

Safe to delete, or keep as an in-repo signpost.
