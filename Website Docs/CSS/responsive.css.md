# responsive.css

**Role**: leftover global media queries — most responsive rules now live next to their component ([[cards.css]] reels, [[navigation.css]] fixed transparent nav). (32 lines)

- **≤ 768 px**: hide `.nav-links`, show `.hamburger` (the desktop→mobile nav switch, paired with [[navigation.css]]); enlarge `.hero h1` to 2.5rem; stack `.card-grid` vertically
- **≤ 480 px**: shrink `.hero-fullscreen` padding to 30 px and its heading to 1.5rem ([[hero.css]])

See [[CSS Overview]].
